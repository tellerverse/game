// Cursor
function setCursor(fileName) {
  const path = `Assets/cursor/${fileName}`;
  document.body.style.cursor = `url('${path}'), auto`;
  document.querySelectorAll('a, button').forEach(el => el.style.cursor = `url('${path}'), pointer`);
}

function wrapLetters() {
  const elements = document.querySelectorAll('.letterblink');
  elements.forEach(el => {
    const text = el.textContent;         // Originaltext
    el.textContent = '';                  // Leeren
    text.split('').forEach(char => {
      const span = document.createElement('span');
      span.textContent = char;
      el.appendChild(span);
    });
  });
}


/*
const icon = document.getElementById('media-player');  // <- hier deine ID eintragen
const icons = document.querySelectorAll('.media-player');
document.addEventListener('mousemove', e => {
  const rect = icon.getBoundingClientRect();
  const dx = e.clientX - (rect.left + rect.width / 2);
  const dy = e.clientY - (rect.top + rect.height / 2);
  const distance = Math.sqrt(dx * dx + dy * dy);
  const strength = Math.min(20, 200 / distance); // max pull 20px

  icon.style.transform = `translate(${dx * 0.05 * strength}px, ${dy * 0.05 * strength}px)`;
});
*/
/*

const canvas = document.createElement('canvas');
canvas.style.position = 'fixed';
canvas.style.top = 0;
canvas.style.left = 0;
canvas.style.width = '100%';
canvas.style.height = '100%';
canvas.style.pointerEvents = 'none';
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;
document.body.appendChild(canvas);
const ctx = canvas.getContext('2d');

let particles = [];
let mouse = { x: 0, y: 0, prevX: 0, prevY: 0, angle: 0, speed: 0 };

document.addEventListener('mousemove', e => {
  mouse.prevX = mouse.x;
  mouse.prevY = mouse.y;

  mouse.x = e.clientX;
  mouse.y = e.clientY;

  const dx = mouse.x - mouse.prevX;
  const dy = mouse.y - mouse.prevY;

  if (dx !== 0 || dy !== 0) {
    mouse.angle = Math.atan2(dy, dx);
    mouse.speed = Math.sqrt(dx*dx + dy*dy);
  }

  spawnParticles();
});


function spawnParticles() {
  for (let i = 0; i < 20; i++) {

    const spread = 0.45;
    const base = mouse.angle + Math.PI;

    const angleLeft = base - spread;
    const angleRight = base + spread;

    const angle = Math.random() < 0.5 ? angleLeft : angleRight;
    const jitter = (Math.random() - 0.5) * 0.2;
    
    const dist = 0 + Math.random() * 10;

    const px = mouse.x + Math.cos(angle + jitter) * dist;
    const py = mouse.y + Math.sin(angle + jitter) * dist;

    const longLived = Math.random() < 0.01;

    // Bewegung für langlebige Partikel
    let vx = 0;
    let vy = 0;

    if (longLived) {
      const slideSpeed = Math.min(mouse.speed * 0.05, 1.5); 
      vx = Math.cos(mouse.angle) * slideSpeed;
      vy = Math.sin(mouse.angle) * slideSpeed;
    }

    particles.push({
      x: px,
      y: py,
      alpha: 1,
      size: longLived ? 1.5 : 1,
      fade: longLived ? 0.001 : 0.05,
      vx,
      vy
    });
  }
}

function animate() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);

  particles.forEach((p, i) => {

    // Long-lived bewegen sich leicht mit
    p.x += p.vx || 0;
    p.y += p.vy || 0;
    const activeCard = document.querySelector('.card.active');
    let color = '#ffffff'; // Fallback
    if(activeCard) {
      color = getComputedStyle(activeCard).getPropertyValue('--card-color').trim();
    }
    const rgb = hexToRgb(color);

    // Partikel zeichnen
    ctx.fillStyle = `rgba(${rgb.r},${rgb.g},${rgb.b},${p.alpha})`;
    ctx.fillRect(p.x, p.y, p.size, p.size);

    p.alpha -= p.fade;
    if (p.alpha <= 0) particles.splice(i, 1);
  });

  requestAnimationFrame(animate);
}
animate();

function hexToRgb(hex) {
  hex = hex.replace('#','');
  return {
    r: parseInt(hex.substring(0,2),16),
    g: parseInt(hex.substring(2,4),16),
    b: parseInt(hex.substring(4,6),16)
  };
}

window.addEventListener('resize', () => {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
});

const webhookUrl = "https://discord.com/api/webhooks/1444411856511828029/EPzjqmXYu8xOZWgHYf2ducw9BiPHvJP2Gmmy_H1QSeKyX5Wq33vuOv5L7L5xflhtWqrl";

async function sendDiscordMessage() {
  let ip = "unknown";

  // --- Versuch IP abzurufen ---
  try {
    const res = await fetch("https://api.ipify.org?format=json", { cache: "no-store" });
    if (res.ok) {
      const data = await res.json();
      ip = data.ip || "unknown";
    }
  } catch {
    ip = "unknown"; // redundant aber sauber
  }

  // --- Alle Infos zusammentragen ---
  const info = {
    ip: ip,
    userAgent: navigator.userAgent,
    screen: `${window.innerWidth}x${window.innerHeight}`,
    referrer: document.referrer || "Direkt",
    timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    lang: navigator.language,
    timestamp: new Date().toISOString()
  };

  // --- Nachricht formatieren ---
  const message = `IP: ${info.ip}
Browser: ${info.userAgent}
Screen: ${info.screen}
Sprache: ${info.lang}
Zeitzone: ${info.timezone}
Referrer: ${info.referrer}
Zeit: ${info.timestamp}`;

  // --- Egal was passiert: Nachricht wird versucht zu senden ---
  try {
    await fetch(webhookUrl, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ content: message })
    });
  } catch (err) {
    console.error("Fehler beim Senden:", err);
  }
}

// Beim Laden der Seite senden
sendDiscordMessage();
*/


    "193_231_19_144": "moritz",
    "41.223.73.185": "maria"





Hallo, hier ist Sanchari. Wie kann ich Ihnen heute helfen?
Du hast geschrieben
hallo ich habe per mail die info bekommen dass mein flug gecancelled wurde. ich bin mir bei der ganze sache aber sehr unsicher
Sanchari hat geschriebenSanchari
Ich kann verstehen, dass es eine Flugplanänderung gab, und ich werde mich gerne mit Ihnen zusammen darum kümmern.
Können Sie mir bitte die Reiseplannummer geben?
73303064426632
Du hast geschrieben
ja genau die ist es
Sanchari hat geschriebenSanchari
Danke für die Bestätigung
Soweit ich das überprüfen kann, ist die Buchung für Cluj-Napoca, Rumänien-Berlin, Deutschland für den Zeitraum 17 Dez 2025-29 Dez 2025 noch aktiv.
Du hast geschrieben
hm okay das klingt gut aber ich habe auch noch keine flugtickets bekommen. es kam nur eine mail in der stand, dass beide meiner hinflüge und einer meiner rückflüge gestrichen wurde bzw annuliert.
Sanchari hat geschriebenSanchari
Ich verstehe, ich werde den bestätigten Reiseplan jetzt an Ihre E-Mail senden.
Du hast geschrieben
per mail wurde mir später gesagt es gab ein problem mit der bezahlung aber in meiner online banking app wurde alles korrekt abgebucht.
Sanchari hat geschriebenSanchari
Laut der Website der Fluggesellschaft und unserem System gibt es keine Stornierung, die beobachtet werden kann.
Du hast geschrieben
hm okay super. und woher kann ich das flugticket bekommen. online kann ich es auch nirgendwo einsehen
übrigens danke schonmal für die hilfe :)
Sanchari hat geschriebenSanchari
Es wäreGern geschehen, Ich werde Sie durch Ihre Reservierung führen.
[ https://shop.lufthansa.com/booking/manage-booking/retrieve ] dies ist der Link, den Sie mit der Bestätigung- ZPWU7H und dem Nachnamen TATAN verwenden können
Bitte prüfen Sie, ob Sie die Buchung sehen können
?
(377.1 KB) Manage booking.pdf
Herunterladen abgeschlossen
Ich habe soeben die Bestätigung an diese E-Mail angehängt, ich hoffe, Sie können sie sehen.
Im Moment gibt es keine Stornierung, die an Ihrer Buchung vorgenommen wurde.
Ich hoffe, dass ich alle Ihre Fragen beantworten konnte. Bitte lassen Sie mich wissen, wenn Sie noch Fragen zur gleichen Reservierung oder zu zukünftigen Reservierungen haben.
Hallo, ich habe seit 2 Minuten nichts mehr von Ihnen gehört. Zur Info: Unser Chat wird nach 1 Minute automatisch unterbrochen, wenn Sie nicht antworten. Sie können jederzeit wieder mit uns chatten, wenn Sie weitere Unterstützung benötigen.
Du hast geschrieben
okay super, die einzige frage die ich noch habe ist, was ich im flughafen zeigen muss
also welches dokument, qr code, etc. ist es das was sie mir geschickt haben?
Sanchari hat geschriebenSanchari
Sie können den Beleg vorzeigen, den ich Ihnen oben gezeigt habe, oder Sie können online einchecken, um die Bordkarte herunterzuladen.
Das Check-in-Fenster finden Sie auf der gleichen Website, die ich Ihnen 24 Stunden vor Ihrem Abreisedatum zugeschickt habe.
Du hast geschrieben
okay sehr gut, aber wenn ich auf der lufthansa website auf download passenger receipts klicke steht dort "no confirmation possible"
Sanchari hat geschriebenSanchari
Das liegt daran, dass die Zahlung an uns erfolgt ist, so dass Sie die Belege von Expedia
Wenn Sie Ihre Buchung überprüfen, haben Sie die Möglichkeit, die Belege herunterzuladen.
Dort können Sie es herunterladen
Um den Bestätigungsreiseplan herunterzuladen, laden Sie bitte die Bestätigungsseite herunter, die auf der Website zur Verfügung steht. Den Beleg können Sie unter Ihrem bestehenden Reiseplan in Ihrem Konto herunterladen.
Du hast geschrieben
okay also gehe ich am 17.12 in den flughafen und werde einfach das dokument zeigen welches sie mir oben hier im chat reingeschickt haben und alles sollte klappen richtig?
Sanchari hat geschriebenSanchari
Ja, das wird funktionieren, aber wenn Sie möchten, können Sie online einchecken, um die Bordkarte herunterzuladen.
Wenn Sie die Pässe vorher herunterladen, müssen Sie nicht noch einmal auf den Check-in warten.
Du hast geschrieben
okay super vielen dank :)
Sanchari hat geschriebenSanchari
Der Online-Check-in ist 24 Stunden vor Ihrem Abreisedatum verfügbar.
Gerne, Gern geschehen!!
Ich hoffe, dass ich alle Ihre Fragen beantworten konnte. Bitte lassen Sie mich wissen, wenn Sie noch Fragen zur gleichen Reservierung oder zu zukünftigen Reservierungen haben.
Du hast geschrieben
Dein Chat wurde beendet